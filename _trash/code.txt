# SecondBrain Technical Reference

## 1. TaskService.java Method Signatures
```java
public Task createTask(String title, String priority, String dueDate, User user)
public Task updateTask(Long taskId, String title, boolean completed, String status, String priority, String dueDate, User user)
```

## 2. ActionExecutorService.java Calls
```java
// Case: CREATE_TASK
String title = (String) action.getData().get("title");
String priority = (String) action.getData().get("priority");
String dueDate = (String) action.getData().get("dueDate");
taskService.createTask(title, priority, dueDate, user);

// Case: UPDATE_TASK
Long id = ...;
String title = (String) action.getData().get("title");
Boolean completed = (Boolean) action.getData().get("completed");
String status = (String) action.getData().get("status");
String priority = (String) action.getData().get("priority");
String dueDate = (String) action.getData().get("dueDate");
taskService.updateTask(id, title, completed != null && completed, status, priority, dueDate, user);
```

## 3. Data Models

### Entity: Task.java
```java
private LocalDate dueDate;
private LocalDateTime createdAt;
private LocalDateTime updatedAt;
```

### DTO: TaskDTO.java (Fixing mismatch)
```java
private LocalDate dueDate; // Was LocalDateTime, causing DTOConverter error
private LocalDateTime createdAt;
```

### AI Payload: AiActionDto.java
```java
public class AiActionDto {
    private ActionType type; // Enum: CREATE_TASK, UPDATE_TASK, etc.
    private Map<String, Object> data; // Dynamic map for JSON extraction
}
```

## 4. Exact Compiler Errors
```text
[ERROR] DTOConverter.java:[26,39] incompatible types: java.time.LocalDate cannot be converted to java.time.LocalDateTime
[ERROR] ActionExecutorService.java: (Fixed in previous step - was argument length mismatch)
```
